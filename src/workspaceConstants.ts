/** SVG-Viewbox-Dimensionen des Workspace-Canvas (mm-Einheiten). */
export const VIEWBOX_WIDTH = 800
export const VIEWBOX_HEIGHT = 600
export const VIEWBOX_CX = VIEWBOX_WIDTH / 2
export const VIEWBOX_CY = VIEWBOX_HEIGHT / 2
