/**
 * UI-Zugang zum Konfigurator (Menü „Erzeugen“ → Konfigurator, zugehörige Modale).
 * Code und Store-APIs bleiben erhalten; auf `true` setzen zum Reaktivieren.
 */
export const CONFIGURATOR_UI_ENABLED = false
