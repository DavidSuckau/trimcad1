/**
 * Experimentelle Sitz-3D-Vorschau (Menü „Erzeugen“ → Sitz-3D-Vorschau).
 * `false` = UI aus; Ordner `src/seat3d/` kann danach gelöscht werden (siehe README.md).
 */
export const SEAT_3D_PREVIEW_ENABLED = true
